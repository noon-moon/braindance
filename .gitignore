# Working repos — cloned here locally, never tracked
repo/

# Harness-generated sync targets (created by ctx/tools/sys/sync.sh)
.claude/
.cursor/

# Node
node_modules/
api/dist/

# Host secrets — the real one lives at /srv/.env, never in the repo
.env

# OS
.DS_Store
Thumbs.db
